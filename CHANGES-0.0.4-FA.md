# پهپاد شاهد ۰٫۰٫۴ — گرافیک، سخت‌افزار واقعی، قانون ستاره و رفع باگ

`versionCode = 4` · `versionName = "0.0.4"`

## اول از همه: چه چیزی انجام نشد و چرا

**بیلد و تست روی دستگاه انجام نشده است.** محیط کاری من Gradle، Android SDK و کامپایلر
Kotlin ندارد. این بسته سورس آماده برای CI شماست، نه ادعای بیلد موفق.

**مدل سه‌بعدی از اینترنت دانلود نشده است.** دو دلیل مستقل:

۱. محیط من به اینترنت دسترسی ندارد، پس هیچ فایلی قابل دریافت نبود.
۲. مهم‌تر: موتور این بازی `android.graphics.Canvas` است، یک رندرکنندهٔ نقاش
   (painter) با مرتب‌سازی عمق. نه z-buffer دارد، نه بارگذار مش، نه shader و نه
   OpenGL ES. بارگذاری OBJ/GLTF بدون بازنویسی کامل موتور روی OpenGL ES ممکن نیست و
   آن یک پروژهٔ جداگانه است، نه یک آپدیت.

به‌جای آن **هندسهٔ رویه‌ای با ابعاد واقعی (متر)** ساخته شد؛ نتیجه از دید بازیکن همان
است: تانک شبیه تانک، ناوشکن شبیه ناوشکن. اگر بعداً واقعاً موتور OpenGL خواستی، بگو؛
آن مسیر جداست.

## ۱. بافت‌ها: کل مجموعه از نو ساخته شد

بافت‌های قبلی زمین و مصالح تقریباً تصاویر تار بدون جزئیات بودند (`terrain_desert`
یک لکهٔ قهوه‌ای محو بود و `prop_rock1` عملاً دو خط نازک). همه از نو تولید شدند:

| فایل | تغییر |
|---|---|
| `terrain_desert` | شن با موج بادی دوجهته، تپه، سنگ‌ریزه، بوتهٔ خشک، غبار روی قله |
| `terrain_urban` | دال‌های بتنی با درز، ترک‌های باریک، لکهٔ روغن، وصلهٔ آسفالت |
| `terrain_naval` | موج بلند آرام، چوپی کوتاه، کف موج فقط روی تاج، برق آفتاب |
| `terrain_special` | سنگ آتشفشانی خرد، قلوه‌سنگ، غبار اکسید آهن در گودی‌ها |
| `material_wall` | نمای کامل ساختمان: ۵×۷ پنجرهٔ فرورفته، شیشهٔ بازتابی، چند پنجرهٔ روشن، درز پنل، رگهٔ آب |
| `material_roof` | بام قیر و شفته با جان‌پناه، یونیت‌های تهویه با کرکره و سایهٔ جهت‌دار |
| `material_concrete` | بتن قالب‌چوبی با خط قالب، سوراخ مهار، تراشه، رگهٔ زنگ |
| `material_metal` | **جدید** — ورق فولاد ناوی با درز جوش، پرچ، رگهٔ زنگ |
| `material_deck` | **جدید** — عرشهٔ ضدلغزش با خط‌کشی زرد، دریچه و حلقهٔ مهار |
| `material_camo` | **جدید** — استتار سه‌رنگ کویری با غبار، خط‌وخش و پیچ |
| `material_track` | **جدید** — زنجیر تانک: حلقه، گروسر، ریب عرضی، گِل |
| `material_rust` | **جدید** — پوستهٔ سوخته و زنگ‌زده برای لاشه‌ها |
| `prop_rock1/2` | صخرهٔ واقعی با حجم (distance transform)، ترک، سایهٔ تماس با زمین |
| `prop_tree1/2` | آکاسیای تاج‌پهن و تبریزی ستونی، با نور جهت‌دار |
| `prop_palm` | **جدید** — نخل |
| `prop_bush` | **جدید** — بوتهٔ کویری |
| `shadow_soft` | افت نرم‌تر |
| `hud_target` | نشانگر تیزتر با چهار کمان و ضربدر مرکزی |

همه کاشی‌شو (tileable) هستند: نویز روی شبکهٔ دوره‌ای ساخته می‌شود و گرادیان با
`np.roll` گرفته می‌شود، پس درز کاشی وجود ندارد. همه با نورپردازی جهت‌دار از **همان
خورشید موتور بازی** (`Scene3D.sunX/Y/Z`) پخته شده‌اند، پس بافت و هندسه هم‌جهت‌اند.

اسکریپت تولید در `tools/` است (`texlib.py`، `gen_terrain.py`، `gen_props.py`). هر
بافت با seed ثابت بازتولیدپذیر است.

**حجم:** کل `assets/` از ۳٬۹۹۶٬۲۸۹ به ۴٬۵۴۴٬۱۹۷ بایت رسید (+۵۳۵ کیلوبایت). بافت‌ها
با `pngquant` و `optipng` فشرده شدند؛ PSNR اندازه‌گیری‌شدهٔ فشرده‌سازی ۴۳ تا ۴۶ دسی‌بل
است، یعنی عملاً بی‌افت. سقف ۱۰٬۰۰۰٬۰۰۰ بایتی CI دست‌نخورده ماند. **حجم نهایی APK
اندازه‌گیری نشده است.**

## ۲. سخت‌افزار: تانک، کشتی و پدافند واقعی

دو فایل تازه:

- **`game/Rig.kt`** — ابزار ساخت حجم در دستگاه محلی هر وسیله: `box`، `taper` (هرم
  ناقص)، `hull` (مقطع متغیر در طول محور، برای دماغهٔ کشتی)، `wedge` (سینهٔ شیب‌دار)،
  `tube` (استوانه، برای لوله و دکل و چرخ)، `disc` (بشقاب رادار) و `face`. قرارداد
  چرخش عیناً همان `Scene3D.box` است تا جهت اجسام با محتوای قبلی هم‌خوان بماند. نرمال
  هر وجه نسبت به مرکز حجم به بیرون چرخانده می‌شود، پس ترتیب رأس‌ها نور را خراب نمی‌کند.
- **`game/Vehicles.kt`** — مدل‌ها با ابعاد واقعی:

| مدل | جزئیات |
|---|---|
| تانک (طول ۹٫۸ م) | زنجیر بافت‌دار، ۶ چرخ راهنما + چرخ محرک و هرزگرد، خاک‌انداز، سینهٔ شیب‌دار، برجک چرخان، لولهٔ ۱۲۵ با ترمز دهانه، دریچهٔ فرمانده، تیربار، پرتابگر دودزا، سبد بار، بشکهٔ سوخت، آنتن |
| نفربر ۸×۸ | بدنهٔ باریک‌شونده، دماغهٔ شیب‌دار، برجک تیربار، ۸ چرخ |
| کامیون | کابین، شیشه، چادر بار، ۶ چرخ |
| **پرتابگر موشک** | شاسی ۸×۸، کابین، چهار لولهٔ پرتاب که **با زاویهٔ واقعی نشانه‌روی بالا و پایین می‌رود**، پایهٔ تکیه‌گاه |
| **توپ پدافند** | شاسی زنجیری، برجک چرخان، دو لولهٔ ۳۵ با شعلهٔ دهانه در لحظهٔ شلیک، رادار جست‌وجو و رادار پیگیری |
| **ایستگاه رادار** | ساختمان عملیات، برج خرپای چهارپایه، **آرایهٔ چرخان** با بازوی تعادل و چراغ هوانوردی — روی مرکز هر منطقهٔ رادار |
| **ناوشکن** | بدنه در شش مقطع با باریک‌شدن به دماغه و پاشنه، شیر عرشه، برجک توپ اصلی + لوله، سلول‌های پرتاب عمودی، جزیرهٔ فرماندهی سه‌طبقه با شیشهٔ تیره و آرایهٔ فازی، دودکش مایل، دکل رادار، سلاح نقطه‌زن، آشیانه و سطح فرود بالگرد با دایرهٔ نشانه، نرده، کف موج دماغه |
| **ناو هواپیمابر** | عرشهٔ پرواز با خط فرود زاویه‌دار، خط مرکزی، دو منجنیق، جزیره در سمت راست، رادار، شش جنگندهٔ پارک‌شده با بال دلتا |
| کشتی باری | بدنه، خانهٔ سکان، دودکش، کانتینرهای رنگی |
| دکل خرپا | چهار پایهٔ باریک‌شونده، مهاربندی ضربدری، نوارهای سرخ و سفید هوانوردی، بشقاب و آنتن سکتوری، چراغ |
| سرپناه | خاک‌ریز، بلوک بتنی، سقف ضخیم، روزنهٔ دید، درِ ضدانفجار، کیسهٔ شن، تهویه |
| دهانهٔ تونل | صخره، جرز و سرتاق بتنی، دهانهٔ عمق‌دار، ریل، جادهٔ دسترسی، درِ زرهی کشویی |
| برج کنترل | ساقهٔ باریک، خرپای دور آن، اتاقک آویزدار با نوار شیشه، نرده، بشقاب، چراغ |
| پل | پنج پایه، عرشه، خرپای ضربدری، نرده، خط‌کشی جاده |
| لکوموتیو + واگن | هودِ موتور، کابین، بوژی و چرخ؛ واگن سرپوشیده، تانکر و فلت‌کار با بار موشک |
| ساختمان | پی، بدنه، هرهٔ بام، ورودی شیشه‌ای، سایبان، یونیت بام، آنتن |

**حالت انهدام** برای همه: پوستهٔ سوخته، رنگ زغالی، برجک تانک از محور خارج می‌شود و
کشتی در آب می‌نشیند.

موشک پدافند از مکعب به استوانه با شعلهٔ موتور تغییر کرد.

**کاروان زرهی** (مرحلهٔ ۲-۱) دیگر یک جعبه نیست: تانک هدف با یک نفربر و یک کامیون.
**قطار** لکوموتیو + چهار واگن با انواع مختلف است. **مرحلهٔ ۴-۴** حالا واقعاً ناو
هواپیمابر است (`TargetShape.CARRIER`)، نه یک ناوشکن بزرگ‌شده.

### سطح جزئیات و بودجهٔ فریم

سه سطح بر اساس فاصلهٔ دوربین (۲۵۰ متر / ۸۲۰ متر / دورتر). ساختمان‌های شهری آستانهٔ
تنگ‌تری دارند (۱۷۵ / ۴۲۰ متر) چون تعدادشان به صد می‌رسد؛ در دورترین سطح به همان
جعبهٔ قبلی برمی‌گردند. ناوهای پس‌زمینه یک پله ساده‌تر از هدف اصلی رندر می‌شوند.
شمارش وجه در نمای نزدیک: تانک ۳۴۵، پرتابگر ۲۱۵، توپ ۲۰۷، ناوشکن ۳۹۹، رادار ۲۲۱،
ساختمان ۵۴. یک صحنهٔ نمونه با کاروان سه‌خودرویی و دو سامانهٔ پدافند در حداکثر
جزئیات ۹۵۸ وجه شد. **این‌ها شمارش هندسی‌اند، نه benchmark اندروید.**

### اعتبارسنجی هندسه

چون کامپایل و اجرا ممکن نبود، ریاضی `Rig` و دوربین `Scene3D` در پایتون بازنویسی شد
(`tools/preview/`) و مدل‌های Kotlin با یک مترجم مکانیکی به پایتون تبدیل و رستر شدند.
همهٔ مدل‌ها با همین مسیر بازبینی چشمی شدند و چهار مورد (ایستگاه رادار، سرپناه، دهانهٔ
تونل، برج) بر اساس آن بازطراحی شدند. این جای اجرا روی اندروید را **نمی‌گیرد**، اما
خطاهای فاحش هندسی را می‌گیرد.

## ۳. قانون ستاره: ساده‌تر و شفاف

### مشکل قبلی

ستاره به مجموع امتیاز وصل بود: سقف ۲۵۰۰ و برای سه ستاره **۲۲۰۰ + صفر آسیب**. عملاً
باید دقت نزدیک کامل، زمان نزدیک بهینه و **همهٔ بونوس‌های مخفی** را می‌گرفتی. هیچ‌جا
هم نوشته نبود ستاره بر چه اساس داده می‌شود.

### قانون جدید (`data/StarRules.kt`)

هر ستاره یک شرط مستقل و قابل اندازه‌گیری:

| ستاره | شرط |
|---|---|
| ⭐ ۱ | همهٔ اهداف اصلی منهدم شوند |
| ⭐ ۲ | میانگین دقت برخورد ۵۰٪ یا بیشتر |
| ⭐ ۳ | بدون آسیب پدافندی، **یا** رسیدن زیر زمان مرجع |

- ستارهٔ دوم و سوم بدون ستارهٔ اول شمرده نمی‌شوند.
- **بونوس‌های مخفی دیگر شرط ستاره نیستند** و فقط امتیاز و سکه می‌دهند.
- شرط سوم «یا» است، نه «و»: یا تمیز پرواز کن یا سریع.
- زمان مرجع فرمول باز دارد: `فاصله ÷ سرعت پروازی × ۲٫۱ × تعداد اهداف`. عدد پنهانی
  در کار نیست و ضریب ۲٫۱ سخاوتمندانه است.

### جایی که دیده می‌شود

- **بریفینگ**: پنل «شرط ستاره‌ها» با متن هر سه شرط و زمان مرجع همان مرحله و همان
  پهباد انتخابی — **قبل از پرواز**.
- **صفحهٔ نتیجه**: سه ردیف، هرکدام با عنوان شرط، متن قانون، و **عددی که خودت گرفتی**
  (مثلاً «دقت ۳۸٪» یا «۲ آسیب · زمان ۰۱:۱۲»). ردیف قبول‌شده سبز، ردیف ردشده قرمز.

امتیاز و ریز امتیازها سر جای خودشان ماندند؛ فقط از تصمیم ستاره جدا شدند.

## ۴. باگ‌های رفع‌شده

۱. **ناهم‌خوانی امتیاز در حالت بی‌نهایت** — `finish` یک عدد و `MissionResult.total`
   عدد دیگری می‌داد، پس امتیاز نمایش‌داده‌شده با رکورد ذخیره‌شده یکی نبود. حالا
   `scoreTotal` یک مقدار صریح است و همان هم نمایش داده و هم ذخیره می‌شود.
۲. **سکه گرفتن از لغو ماموریت** — `abort()` از مسیر شکست عبور می‌کرد و فرمول باز هم
   سکه می‌داد. لغو حالا صفر سکه و صفر تجربه دارد؛ شکست فقط پاداش تسلی‌بخش می‌گیرد.
۳. **وابستگی پدافند به نرخ فریم** — `fireMissile` حرکت هدف را با گام ثابت `0.016f`
   تخمین می‌زد، پس روی گوشی ۹۰ یا ۱۲۰ هرتز پدافند دقیق‌تر و روی ۳۰ هرتز کورتر بود.
   حالا از سرعت واقعی (متر بر ثانیه) استفاده می‌کند. تعادل در ۶۰ فریم دست‌نخورده است.
۴. **عبور از دیوار در سرعت بالا** — برخورد مانع فقط نقطهٔ پایانی فریم را آزمایش
   می‌کرد. `hitsPropSwept` مسیر بین دو فریم را تا ۱۲ نمونه جاروب می‌کند.
۵. **نادیده‌گرفتن چرخش مانع** — آزمون جعبه هم‌راستا با محور بود. نقطه حالا به دستگاه
   محلی مانع برده می‌شود. صخره و درخت و بوته هم حجم استوانه‌ای گرفتند نه جعبه.
۶. **شرط تکراری** — در `hitsProp` عبارت `if (py > p.h) continue` دو بار پشت هم بود.
۷. **رشد بی‌پایان پدافند** — در موج‌های بالای حالت بی‌نهایت تعداد سامانه‌ها بی‌سقف
   زیاد می‌شد. سقف ۱۲ گذاشته شد (`World.MAX_AA_SITES`).
۸. **خطر سرریز Int** — `addCoins` و `addXp` جمع را در Long انجام می‌دهند و به
   `Int.MAX_VALUE` محدود می‌شوند. کلید و فرمت ذخیره تغییر نکرد.
۹. **خراب‌شدن رکورد زمان** — یک شکست سریع می‌توانست «بهترین زمان» را خراب کند. رکورد
   مرحله فقط با ماموریت موفق ثبت می‌شود.
۱۰. **شمارهٔ نسخه** — منوی اصلی «۰٫۰٫۲» را ثابت نشان می‌داد در حالی که Gradle چیز
    دیگری بود. هر دو حالا ۰٫۰٫۴ هستند.
۱۱. **رقم لاتین در عنوان هدف فرعی** — `"هدف فرعی ${index+1}"` از `Fa.num` عبور
    نمی‌کرد و رقم لاتین نشان می‌داد.
۱۲. **کم‌رنگی بافت** — بافت وجوه جامد با آلفای ۱۵۵ روی رنگ پایه ترکیب می‌شد و جزئیات
    را می‌شست. به ۲۰۸ رسید تا بافت‌های تازه واقعاً دیده شوند.

## ۵. چیزهایی که عمداً دست نخوردند

- فرمت و کلیدهای `SharedPreferences` (`pahpad_shahed_save`): ذخیرهٔ قبلی بازیکن سالم
  می‌ماند. ستاره‌های قبلی پاک نمی‌شوند؛ چون قانون تازه آسان‌تر است، بازی مجدد فقط
  می‌تواند ستاره اضافه کند.
- `applicationId`، تنظیمات امضا، مجوزها، `AndroidManifest.xml` و کل `res/`.
- محدودهٔ لمس، جوی‌استیک، تیلت، گاز و محل دکمه‌های HUD.
- مدل پرواز، سوخت، رادار، جنگ الکترونیک، باد و مشخصات پهبادها.
- تصاویر `env_*`، `menu_hero` و `drone_*` که از قبل کیفیت خوبی داشتند.

## ۶. محدودیت‌های باقی‌مانده

- رندرکنندهٔ Canvas همچنان z-buffer ندارد. وجوه متقاطع در بعضی زوایا ترتیب درست
  ندارند. با هندسهٔ جزئی‌تر این احتمال کمی بیشتر از قبل است.
- شیب و کج‌شدن (list/roll) برای کشتی آسیب‌دیده پیاده نشد؛ `Rig` فقط چرخش حول محور Y
  دارد. کشتی منهدم پایین می‌نشیند و سیاه می‌شود، اما کج نمی‌شود.
- بافت روی احجام با `setPolyToPoly` می‌نشیند، پس پرسپکتیو بافت روی وجوه بزرگ و مایل
  کاملاً درست نیست. این محدودیت از قبل بود.
- چیدمان بریفینگ اگر ارتفاع صفحه کم باشد بخش‌هایی را حذف می‌کند تا هم‌پوشانی نشود.
  روی نمایشگر کوتاه ممکن است پنل راهنما نمایش داده نشود.
- افت فریم، باتری و حرارت اندازه‌گیری نشده‌اند.

## ۷. چک‌لیست تست روی گوشی

- [ ] در CI ابتدا `assembleDebug` و سپس `lintDebug` موفق شوند و APK زیر ۱۰٬۰۰۰٬۰۰۰ بایت باشد.
- [ ] روی نصب قبلی با همان امضا ارتقا بده و سکه، تجربه، ستاره‌ها و ارتقاها را قبل و بعد مقایسه کن.
- [ ] چهار محیط را از ارتفاع کم پرواز کن: تکرار بافت زمین، موج آب و ترک آسفالت را ببین.
- [ ] در مرحلهٔ ۲-۱ کاروان را از نزدیک ببین: زنجیر، چرخ، برجک و لولهٔ تانک.
- [ ] در مرحلهٔ ۲-۵ و ۴-۴ ناوشکن و ناو هواپیمابر را از سه فاصله ببین (گذر سطح جزئیات).
- [ ] وارد بردِ پدافند شو: لانچر باید بچرخد و **بالا بیاید**؛ توپ باید شعلهٔ دهانه بزند.
- [ ] در منطقهٔ رادار، آرایهٔ ایستگاه باید بچرخد.
- [ ] بریفینگ: پنل «شرط ستاره‌ها» و زمان مرجع را ببین؛ با تغییر پهباد زمان مرجع باید عوض شود.
- [ ] یک ماموریت را با دقت پایین تمام کن: باید یک ستاره بگیری و دلیلش در صفحهٔ نتیجه قرمز باشد.
- [ ] یک ماموریت را بدون آسیب تمام کن: ستارهٔ سوم باید بیاید حتی اگر بونوسی نگرفته باشی.
- [ ] ماموریت را لغو کن: سکه و تجربه باید صفر باشند.
- [ ] در حالت بی‌نهایت امتیاز صفحهٔ نتیجه را با رکورد ثبت‌شده مقایسه کن؛ باید یکی باشند.
- [ ] با سرعت حداکثر و بوست به دیوار شهری بزن: باید برخورد ثبت شود، نه عبور.
- [ ] روی گوشی ضعیف و در شهر شلوغ نرخ فریم را ببین؛ اگر افت داشت آستانه‌های
      `WorldRenderer.buildingLod` را تنگ‌تر کن.

## ۸. فایل‌های تغییرکرده

| فایل | تغییر |
|---|---|
| `game/Rig.kt` | **جدید** — ابزار ساخت حجم |
| `game/Vehicles.kt` | **جدید** — همهٔ مدل‌های سخت‌افزار |
| `data/StarRules.kt` | **جدید** — قانون ستاره |
| `game/Vehicles.kt`, `game/WorldRenderer.kt` | اتصال مدل‌ها، سطح جزئیات، موشک استوانه‌ای |
| `game/World.kt` | نوع سامانهٔ پدافند، زاویهٔ ارتفاع، چرخش رادار، `CARRIER`، برخورد جاروب‌شده و چرخش‌آگاه، سقف پدافند، نخل و بوته، ناو و کشتی باری |
| `game/Mission.kt` | امتیاز واحد، ستاره از `StarRules`، سرعت مستقل از فریم، پاداش لغو و شکست، میانگین دقت |
| `game/Scene3D.kt` | آلفای بافت وجوه جامد |
| `core/SaveManager.kt` | جلوگیری از سرریز Int |
| `screens/ResultScreen.kt` | نمایش سه قانون ستاره با مقدار واقعی |
| `screens/BriefingScreen.kt` | پنل شرط ستاره‌ها، چیدمان مقاوم به ارتفاع کم |
| `screens/MainMenuScreen.kt` | شمارهٔ نسخه |
| `app/build.gradle.kts` | `versionCode = 4`، `versionName = "0.0.4"` |
| `assets/gfx/*` | ۲۰ بافت بازتولید یا اضافه‌شده |
| `tools/*` | اسکریپت تولید بافت و ابزار پیش‌نمایش هندسه |
