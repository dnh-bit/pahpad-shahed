# گزارش بازبینی و اصلاحات سورس «پهباد شاهد»

## وضعیت تحویل

این بسته از روی زیپ ارسالی نسخهٔ ۰٫۰٫۳ ساخته شده است. نام این گزارش طبق درخواست `CHANGES-0.0.4.md` است، اما **نسخهٔ برنامه تغییر نکرده است**.

**بیلد، lint، اجرای اندروید و تست روی گوشی در این محیط انجام نشده‌اند.** محیط Gradle، Android SDK و کامپایلر Kotlin ندارد و به اینترنت هم دسترسی ندارد. بنابراین این بسته سورسِ بازبینی‌شده برای اجرای CI شماست، نه ادعای بیلد موفق یا تضمین رفع همهٔ باگ‌ها. فایل `REVIEW-VALIDATION.json` نتیجهٔ کنترل‌های ایستا و آزمون‌های عددی مستقل را ثبت می‌کند؛ این کنترل‌ها جای کامپایل و تست دستگاه را نمی‌گیرند.

تمام فایل‌های Kotlin اولیه، تنظیمات بیلد و چرخهٔ ارتباط صفحه‌ها بررسی شدند. یک فایل Kotlin جدید برای تولید موج موتور اضافه شده است. تغییرات اصلاحی به صدا، نمایش، ورودی و چرخهٔ عمر محدود شده‌اند؛ ایرادهای مربوط به امتیاز و ذخیره به دلیل محدودیت‌های صریح شما فقط گزارش شده‌اند.

## مواردی که دست‌نخورده مانده‌اند

- کل `app/build.gradle.kts` بایت‌به‌بایت با ورودی برابر است: `applicationId = ir.shahed.pahpad`، `versionCode = 3` و `versionName = "0.0.3"`، تنظیمات امضا، مسیر و نحوهٔ خواندن keystore و وابستگی‌ها تغییر نکرده‌اند.
- `build.gradle.kts`، `settings.gradle.kts`، `gradle.properties` و `gradle/wrapper/gradle-wrapper.properties` تغییر نکرده‌اند.
- `AndroidManifest.xml` و تمام منابع `res/` ثابت مانده‌اند؛ هیچ مجوز، حسگر یا وابستگی جدیدی اضافه نشده است.
- `data/Levels.kt`، `data/DroneModels.kt`، `data/Ranks.kt`، `game/Mission.kt`، `game/World.kt` و `core/SaveManager.kt` بایت‌به‌بایت ثابت‌اند. فرمول امتیاز، ستاره، اقتصاد، برخورد، مشخصات مراحل و مدل پرواز تغییر نکرده است.
- ذخیرهٔ موجود از SharedPreferences با نام `pahpad_shahed_save` استفاده می‌کند. نام، کلیدها، فرمت و مسیر ذخیره و پیشرفت دست‌نخورده‌اند؛ مهاجرت یا پاک‌سازی ذخیره اضافه نشده است.
- محدودهٔ چپ و راست لمس، نسبت کشیدن گاز، اندازهٔ مؤثر جوی‌استیک و محل دکمه‌های HUD ثابت‌اند.
- تمام فایل‌های تصویری و دارایی‌های اولیه بدون تغییر نگه داشته شده‌اند. متن فارسی جدیدی به UI اضافه نشده است.
- `art-source/`، `tools/`، `preview/`، `checks/` و `GRAPHICS-UPDATE-FA.md` در ورودی دست‌نخورده‌اند و در بستهٔ خروجی وجود ندارند.
- APK، فایل امضا، خروجی بیلد یا کتابخانهٔ باینری تازه تحویل داده نشده است.

## ۱. دود موتور

### ریشهٔ مشکل

دود قدیمی با احتمال ۰٫۵۵ در هر فریم ساخته می‌شد؛ تعداد آن به نرخ فریم وابسته بود. اندازه و رشد زیاد، عمر نسبتاً طولانی و رنگ کدر باعث پوشیده‌شدن صحنه می‌شد. علاوه بر آن، `Theme.withAlpha` مقدار آلفای رنگ را جایگزین می‌کند؛ بنابراین فقط کم‌کردن آلفای رنگ در `smoke()` مشکل شفافیت را به‌تنهایی حل نمی‌کرد.

### اصلاح

- دود موتور از دود آتش‌سوزی هدف جدا شد: `Fx.engineSmoke` برای موتور و `Fx.smoke` برای لاشهٔ هدف است.
- تولید دود موتور به زمان‌سنج نمایش منتقل شد: تقریباً یک ذره هر ۰٫۱۲ ثانیه و هنگام بوست هر ۰٫۰۷۵ ثانیه.
- آلفای آغازین حدود ۳۱٪ در حالت عادی و ۴۰٪ در بوست است و هنگام رسم در ضریب محوشدن ضرب می‌شود، نه اینکه جایگزین شود.
- اندازهٔ آغازین به ۰٫۴۲ و ۰٫۵۵ واحد، رشد به ۰٫۵۵ واحد بر ثانیه و عمر به حدود ۰٫۴۲ تا ۰٫۶۲ ثانیه کاهش یافت. محوشدن دود موتور منحنی درجهٔ دوم دارد.
- نقطهٔ تولید عقب‌تر از بدنه قرار گرفت. در FPV تولید دود موتور قطع و ذرات قبلی آن پاک می‌شوند؛ رسم نیز محافظ جداگانهٔ FPV دارد. دود انفجار همچنان قابل مشاهده است.
- برای ثابت‌ماندن رفتار بازی، کد `Mission.kt` و فراخوانی مولد تصادفی آن تغییر نکرده‌اند. فقط دریافت‌کنندهٔ قدیمی `onSmoke` غیرفعال است و تولید بصری از `WorldRenderer.update` انجام می‌شود.
- تولید دود لاشه و جرقه از `draw()` به `update()` منتقل شد. رسم مجدد در حالت توقف دیگر ذرهٔ جدید نمی‌سازد.

## ۲. صدای موتور و افکت‌ها

### فایل تازه: `core/EngineWave.kt`

صدای موتور یک بازطراحی هنری برای بازی است، نه ضبط واقعی یا بازسازی تأییدشدهٔ صدای یک پهپاد مشخص.

- موج یک‌ثانیه‌ای با نرخ نمونهٔ ۲۲٬۰۵۰ هرتز و پایهٔ اسمی ۱۰۰ هرتز ساخته می‌شود.
- موج دندانه‌اره‌ای نرم با شش هارمونیک، مؤلفهٔ بم، پالس ملخی و نویز محدود به باند با ترکیب مؤلفه‌های سینوسی اضافه شده‌اند.
- تغییرات کوچک و دوره‌ای فاز، صدای ثابت و الکترونیکی حلقهٔ قبلی را کاهش می‌دهد. تمام مؤلفه‌ها دورهٔ مشترک دارند تا مرز حلقه پیوسته بماند.
- شدت موتور نرخ پخش را نرم تغییر می‌دهد؛ پایهٔ اسمی از ۸۰ تا ۱۲۰ هرتز می‌رود و ریتم پالس نیز همراه آن تغییر می‌کند. لرزش فرکانس جزئی می‌تواند لحظه‌ای کمی پیرامون این بازه حرکت کند.
- مقدار ورودی صدا از سرعت موجود بازی گرفته می‌شود؛ این تغییر فقط صوتی است.
- افزایش و کاهش ولوم در شروع و توقف موتور تدریجی است. نمونهٔ صوتی خارجی و crossfade بین چند ضبط اضافه نشده است.

### ایرادهای صوتی اصلاح‌شده در `GameAudio.kt`

۱. **تولید نمونه و ساخت موتور روی ترد رابط:** تولید موج، ساخت AudioTrack، نوشتن PCM و آزادسازی ترک‌ها به workerهای پس‌زمینه منتقل شدند. ترد اصلی فقط فرمان و شدت صدا را ثبت می‌کند.

۲. **صف نامحدود افکت‌ها:** صف افکت‌ها به دوازده کار منتظر و سه worker محدود شده است. در اشباع، افکت تازه حذف می‌شود تا کارهای صوتی روی هم انباشته نشوند. این سیاست عمداً ثبات عملکرد را به پخش همهٔ افکت‌ها ترجیح می‌دهد.

۳. **ملودی‌های متداخل و تأخیر صف:** موفقیت و شکست اکنون هرکدام یک کلیپ واحد هستند؛ worker دیگر هنگام خوابیدن کار تازه‌ای به صف خودش اضافه نمی‌کند.

۴. **باقی‌ماندن کارهای قدیمی هنگام توقف و خروج:** شناسهٔ نسل و صف محدود برای موتور اضافه شد. کارهای منقضی شروع نمی‌شوند و در پایان به‌صورت پس‌زمینه آزاد می‌شوند. توقف و بازگشت برنامه، افکت‌های قدیمی را نیز لغو می‌کند.

۵. **عدم آزادسازی ترک در شکست ساخت یا تنظیم:** مسیرهای شکست و پایان، `AudioTrack` را در همان worker آزاد می‌کنند. بازگشت نوشتن PCM بررسی می‌شود و نوشتن ناقص ادامه می‌یابد. برای حالت STATIC، وضعیت قبل و بعد از نوشتن به‌درستی بررسی شده است.

۶. **پرش مرز افکت‌های کوتاه:** برای ابتدا و انتهای کلیپ‌ها رمپ حدود چهار میلی‌ثانیه اضافه شد. فاز افکت‌های دارای تغییر فرکانس با جمع‌کردن گام فاز تولید می‌شود.

۷. **ادامهٔ موتور بین پرتاب‌ها:** در فاز آماده یا پایان، موتور متوقف می‌شود؛ پرتاب بعدی آن را دوباره راه می‌اندازد.

تمام این موارد هنوز به تست شنیداری روی دستگاه واقعی نیاز دارند. زمان خروج از ترک فعال معمولاً در محدودهٔ چرخهٔ بررسی worker و رمپ خروج است، اما زمان‌بندی سیستم و درایور صدا قابل تضمین نیست.

## ۳. انفجار، نور و گرافیک

### انفجار

برخلاف توضیح پرامپت، سورس ارسالی از قبل `mission.shake` و فلش تمام‌صفحه داشت. آن‌ها از صفر «اضافه» نشده‌اند، بلکه بازتنظیم شدند:

- لرزش انفجار اکنون حدود ۰٫۲ تا ۰٫۳۸ ثانیه است و دامنه با توان انفجار متناسب می‌شود. افت دامنه درجهٔ دوم است و تغییر فقط روی Canvas اعمال می‌شود.
- لرزش آسیب پدافندی موجود حفظ شده، اما شدت نمایشی آن کمتر است. هنگام توقف، لرزش صحنه اعمال نمی‌شود.
- فلش گرم محلی و فلش کوتاه صحنه حدود ۰٫۰۵ ثانیه دوام دارند؛ در ۶۰ فریم تقریباً دو تا سه فریم. روی نرخ فریم پایین تعداد فریم کمتر می‌شود.
- فلش قبل از HUD رسم می‌شود تا متن و دکمه‌ها سفید و ناخوانا نشوند.
- موج انفجار روی صفحهٔ زمین گسترش پیدا می‌کند و محو می‌شود؛ پیش از هندسهٔ جامد رسم می‌شود تا روی ساختمان‌ها نقاشی نشود. جادهٔ روی آن ممکن است بخشی از حلقه را بپوشاند؛ این محدودیت رندر ساده است.
- هشت افکت موج همزمان و همان ظرفیت ۴۲۰ ذره حفظ شده‌اند. سقف ظرفیت عمداً محدود است.

### پهپاد

- نور بدنه از جهت مشترک خورشید و نرمال وجه‌ها محاسبه می‌شود، نه از یک روشنایی ثابت وابسته به شمارهٔ وجه.
- دو ردیف درز پنل باریک روی بدنه اضافه شده است.
- زاویهٔ ملخ با زمان به‌روزرسانی جمع می‌شود و در هر دور محدود می‌ماند؛ تغییر گاز، زاویه را ناگهان از نو محاسبه نمی‌کند.
- در مدل‌های دارای ملخِ نمایشی، محوشدگی حلقه‌ای کم‌شفافیت با یک بافت محاسباتی ۶۴×۶۴ اضافه شده است. فقط یک بار برای هر رندرکننده ساخته می‌شود.
- سایهٔ پهپاد پهن‌تر و کم‌رنگ‌تر می‌شود و جهت آن با خورشید مشترک هماهنگ است. هیچ بعد برخوردی تغییر نکرده است.

### محیط

- آسمان سه‌رنگ با تن‌های متفاوت برای کویر، شهر، دریا و محیط ویژه و هالهٔ نرم خورشید اضافه شده است. محل خورشید با جهت دوربین تغییر می‌کند.
- نور مکعب‌ها، بدنهٔ پهپاد و جهت سایه‌ها از خورشید مشترک استفاده می‌کنند. نور ازپیش‌رسم‌شدهٔ تصاویر PNG عیناً حفظ شده و نورپردازی کامل پویا یا PBR نیست.
- لایهٔ جزئیات کم‌شفافیت با دوره‌ای متفاوت از بافت اصلی زمین، در مختصات جهان قفل شده است تا تکرار بافت کمتر به چشم بیاید. این کار همهٔ الگوهای تکرار را حذف نمی‌کند.
- گرادیان‌ها، بافت جزئیات و نوار افق cache می‌شوند؛ در هر فریم Bitmap تازه‌ای ساخته نمی‌شود.

### HUD

- پس‌زمینهٔ ارتفاع و سرعت، فاصله و سوخت یکدست‌تر و خواناتر شده است؛ محل نوشته‌ها و محدودهٔ لمس جابه‌جا نشده است.
- نشانگر هدف اکنون داخل همان تبدیل چرخش و لرزش صحنه رسم می‌شود؛ قبلاً تصویر جهان می‌چرخید ولی نشانگر از هدف جدا می‌ماند. متن همراه نشانگر نیز با صحنه می‌چرخد.
- برش نقشهٔ کوچک از مربع به دایره تبدیل شده تا مناطق نقشه از گوشه‌های قاب بیرون نزنند.
- شفافیت ورودی متن در `Ui.text` حفظ می‌شود و با آلفای انیمیشن ترکیب می‌شود.

## ۴. ورودی، چرخهٔ عمر و عملکرد

### ورودی

- دکمه‌ها شناسهٔ انگشت خودشان را نگه می‌دارند؛ حرکت یا رهاکردن انگشت دیگر دکمهٔ اشتباه را فعال نمی‌کند.
- `ACTION_CANCEL` تمام وضعیت‌های لمس را آزاد می‌کند. توقف، خروج و جداشدن View نیز لمس‌های قدیمی را پاک می‌کنند.
- انگشت جدید، مالکیت جوی‌استیک، گاز یا بوستِ نگه‌داشته‌شده را نمی‌دزدد.
- پس از توقف، ورودی هدایت و بوست گیر نمی‌کنند. کالیبراسیون تیلت هنگام ادامه بازنشانی می‌شود.
- در دستگاه فاقد شتاب‌سنج، همان جوی‌استیک موجود قابل استفاده است، بدون افزودن مجوز یا تغییر تنظیم ذخیره‌شده.
- ثبت تکراری حسگر جلوگیری شده است. حسگر معیوب یا شکست ثبت با وجود سخت‌افزار همچنان باید روی دستگاه بررسی شود.

### چرخهٔ عمر

- `BaseScreen` بعد از توقف حلقه، در یک رسمِ ازقبل‌صف‌شده شبیه‌سازی را جلو نمی‌برد.
- در نابودی اکتیویتی، حلقه، صفحه، حسگر، صدا و ارجاع‌های cache آزاد می‌شوند.
- انتقال تأخیری به نتیجه فقط وقتی اجرا می‌شود که همان صفحه هنوز صفحهٔ جاری باشد.
- renderer هنگام خروج، ارجاع‌های shader، face، skyline و ذرات را پاک می‌کند.

### حافظه و عملکرد

- cache جداگانهٔ Bitmap در هر `Ui` حذف و از `Gfx` مشترک استفاده شده است. قبلاً یک تصویر می‌توانست در رابط و رندر جهان جداگانه decode شود.
- از `Bitmap.recycle()` روی تصویرهای مشترک استفاده نشده تا Canvas یا shader با Bitmap بازیافت‌شده مواجه نشود. حذف ارجاع‌ها به GC واگذار شده است؛ این اصلاح به معنی اثبات نبود هر نوع نشت حافظه نیست.
- ساخت آرایهٔ گوشه‌ها برای هر سایه، ساخت Paint و RectF نشانگر در هر فریم و ساخت لیست موقت `input.take(4)` برای هر وجه حذف شده‌اند.
- مقایسه‌گرهای مرتب‌سازی دوباره استفاده می‌شوند؛ ذرات بر اساس عمق دوربین مرتب می‌شوند و عمق داخل مقایسه‌گر دوباره محاسبه نمی‌شود.
- وجه‌های خارج کادر جای تازه‌ای در pool مصرف نمی‌کنند. گرادیان‌های پس‌زمینهٔ Ui نیز cache شده‌اند.
- scroll لیدربورد دیگر به عدد ثابت ۲۲۰dp محدود نیست؛ اندازهٔ محتوا و پنجره محاسبه می‌شود تا آخرین مرحله قابل دسترسی باشد. مالکیت انگشتِ اسکرول نیز مشخص شده است.
- `Fa.num` و `Fa.clock` از Locale ثابت برای قالب‌بندی اولیه استفاده می‌کنند تا ارقام و اعشار زیر تنظیم زبان‌های مختلف از مسیر تبدیل فارسی عبور کنند.

## ۵. فونت وزیرمتن

فایل معتبر وزیرمتن در بستهٔ ارسالی یا محیط حاضر موجود نبود و دانلود از این محیط ممکن نیست؛ **فونت اضافه نشده و فونت سیستم حفظ شده است**.

`Fonts.regular` اکنون ابتدا `assets/fonts/Vazirmatn-Regular.ttf`، سپس مسیر قبلی `assets/fonts/vazirmatn.ttf` و در نهایت فونت سیستم را بررسی می‌کند. اگر بعداً یک فایل معتبر و مجاز از وزیرمتن اضافه کنید، همین مسیر جدید بدون تغییر کد استفاده می‌شود. بولد در نبود فایل قدیمی بولد، از همان فونت معمولی ساخته می‌شود. تغییر امروز، وابستگی یا فایل فونت اضافی ندارد.

## ۶. ایرادها و محدودیت‌های باقی‌مانده

این بخش عمداً از موارد اصلاح‌شده جداست. ادعای «بدون باگ» یا «همه‌چیز تست‌شده» مطرح نمی‌شود.

### مواردی که به دلیل ممنوعیت تغییر منطق یا متن دست نخورده‌اند

- در حالت بی‌نهایت، `Mission.finish` امتیاز کل متفاوتی از جمع فیلدهای `MissionResult.total` محاسبه می‌کند؛ بنابراین امتیاز نمایش داده‌شده و رکورد ذخیره‌شده ممکن است متفاوت باشند. اصلاح این مورد به قرارداد امتیاز مربوط است و انجام نشده است.
- مسیر `Mission.abort` به `finish(false, ...)` می‌رسد و فرمول فعلی حتی برای شکست می‌تواند سکه بدهد. تغییر اقتصاد بازی مجاز نبود.
- `SaveManager.addCoins` و `addXp` جمع از نوع Int دارند؛ در مقادیر بسیار بزرگ خطر سرریز باقی است. خود فایل ذخیره طبق دستور تغییر نکرد.
- تعداد دفاع‌ها در موج‌های بسیار بالای حالت بی‌نهایت رشد می‌کند؛ محدودکردن آن تغییر سختی است و انجام نشده است.
- در `Mission.fireMissile` تخمین حرکت از ثابت `0.016f` استفاده می‌کند؛ وابستگی به نرخ فریم در منطق بازی باقی مانده است. رفتار پدافند تغییر نکرده است.
- برخورد موانع در `World.hitsProp` با جعبهٔ هم‌راستا با محور انجام می‌شود و چرخش تصویری بعضی موانع را لحاظ نمی‌کند. همچنین برخورد موانع، برخلاف هدف‌ها، مسیر کامل بین دو فریم را آزمایش نمی‌کند. این قرارداد برخورد تغییر نکرده است.
- عنوان نسخه در منوی اصلی از قبل «۰٫۰٫۲» به‌صورت ثابت بود، در حالی که Gradle نسخهٔ «۰٫۰٫۳» دارد. برای رعایت ممنوعیت تغییر شمارهٔ نسخه و متن، آن رشته عوض نشده است.
- متن چالش روزانه از محدودیت روزانه می‌گوید، اما مسیر شروع آن در سورس اجازهٔ تکرار می‌دهد؛ متن و قواعد مرحله تغییر نکرده‌اند.
- قالب عددی عنوان هدف فرعی در `World.kt` از ابتدا از `Fa.num` استفاده نمی‌کرد. چون فایل جهان عیناً حفظ شده است، این مورد قدیمی هم باقی است.

### محدودیت‌های اجرایی و گرافیکی

- رمزگشایی نخستین تصویرها و ساخت نخستین نوار افق هنوز می‌تواند در اولین نمایش روی ترد اصلی انجام شود. حذف decode تکراری انجام شده، اما preload کامل پس‌زمینه اضافه نشده است.
- Canvas painter renderer همچنان z-buffer ندارد؛ هم‌پوشانی ذرات و وجوه تقاطع‌دار در بعضی زوایا کاملاً صحیح نیست. اسپرایت نزدیک صفحهٔ برش ممکن است به‌جای کلیپ دقیق ناپدید شود؛ تبدیل آن به مستطیل کدر انجام نمی‌شود.
- همهٔ منوهای قدیمی برای اندازه‌های کوچک بازطراحی نشده‌اند. چیدمان‌های ثابت در بریفینگ، فروشگاه و بعضی کارت‌ها ممکن است روی نمایشگر کوتاه هم‌پوشانی داشته باشند؛ محل کنترل‌ها و متن‌ها طبق درخواست حفظ شده است.
- سرعت شبیه‌سازی در نرخ‌های کمتر از ۲۰ فریم تحت clamp قدیمی `dt` قرار می‌گیرد؛ این رفتار تغییر نکرده است.
- افت فریم، مصرف باتری، گرمای گوشی و heap واقعی اندازه‌گیری نشده‌اند. اعداد مربوط به موج و تولید دود، آزمون عددی مستقل‌اند نه benchmark اندروید.

## ۷. بررسی‌های انجام‌شده

- مقایسهٔ بایت‌به‌بایت فایل‌های محافظت‌شده، دارایی‌ها و منابع با زیپ ورودی.
- بررسی وجود تمام فایل‌های لازم ورودی به‌جز مسیرهای ممنوع و نبود APK یا خروجی بیلد در بسته.
- بررسی XMLها و توازن واژگانی پرانتزها، آکولادها و براکت‌ها در فایل‌های Kotlin؛ این بررسی parser یا type checker نیست.
- مرور دستی اتصال متدهای جدید، وضعیت STATIC در AudioTrack، مدیریت نسل کار صوتی، پاک‌سازی لمس و ترتیب رندر.
- بازتولید عددی مستقل فرمول موتور با الگوریتم Java Random در Python: فرکانس غالب ۱۰۰ هرتز، پیک PCM برابر ۱۹٬۰۰۰، میانگین صفر و گام مرز حلقه برابر با گام بعدی، هر دو ۹۷۶ واحد. مقدار ریاضی موج در مرز یک ثانیه عملاً صفر بود.
- نمونهٔ آخر الزاماً مساوی نمونهٔ اول نیست؛ در نمونه‌برداری درستِ یک حلقهٔ دوره‌ای، نمونهٔ آخر در زمان یک گام قبل از مرز است. پیوستگی شیب و مقدار مرزی بررسی شده است، نه چسباندن مصنوعی دو نمونهٔ مساوی.
- بازتولید زمان‌سنج دود در نرخ‌های ۲۰، ۳۰، ۶۰، ۹۰ و ۱۲۰ فریم: در ده ثانیهٔ شبیه‌سازی، حالت عادی ۸۳ و بوست ۱۳۳ ذره تولید کرد.
- جزئیات کنترل‌ها و نتیجه‌ها در `REVIEW-VALIDATION.json` آمده است. هیچ‌کدام تأیید کامپایل یا اجرای AudioTrack نیستند.

## ۸. اجرا در CI و کنترل حجم

ساختار ریشهٔ پروژه و `.github/` حفظ شده است. محتوای پوشهٔ پروژه داخل زیپ را در ریشهٔ ریپوی خود قرار دهید، نه یک پوشهٔ اضافه بالای `app/` و `.github/`.

فرمان فعلی CI همچنان این است:

```bash
gradle assembleDebug lintDebug --no-daemon '-Dorg.gradle.jvmargs=-Xmx3g'
```

ابزارهای مورد نیاز همان تنظیمات موجودند: Java 17، Gradle 8.2، Android SDK 34، Build Tools 34.0.0، Android Gradle Plugin 8.2.2 و Kotlin 1.9.22. بستهٔ اولیه فقط properties مربوط به wrapper را داشت، نه اسکریپت و JAR کامل wrapper؛ فایل تازه‌ای برای wrapper ساخته نشده است. workflow از Gradle نصب‌شده استفاده می‌کند، نه `./gradlew`.

فقط یک گام کنترل حجم به workflow اضافه شده است: اگر APK دیباگ وجود نداشته باشد یا اندازهٔ هر APK به ۱۰٬۰۰۰٬۰۰۰ بایت برسد، CI ناموفق می‌شود. این حد کمی سخت‌گیرانه‌تر از ۱۰MiB است. فایل‌های ورودی این کنترل در محیط بازبینی ساخته نشده‌اند؛ عبور واقعی آن فقط بعد از بیلد مشخص می‌شود.

مجموع فایل‌های assets همان ۳٬۹۹۶٬۲۸۹ بایت ورودی است. تصاویر یا صوت حجیم تازه‌ای در بسته نیست. **حجم APK نهایی اندازه‌گیری و تأیید نشده است.** APK انتشارِ امضاشدهٔ خودتان را نیز جداگانه با همین سقف بررسی کنید؛ workflow فعلی، APK دیباگ را کنترل می‌کند.

## ۹. چک‌لیست تست روی گوشی

- [ ] در CI ابتدا `assembleDebug` و سپس `lintDebug` موفق شوند؛ گزارش lint را جداگانه بررسی کنید.
- [ ] APK حاصل کمتر از ۱۰٬۰۰۰٬۰۰۰ بایت باشد؛ برای release نیز اندازه را جداگانه کنترل کنید.
- [ ] روی نصب قبلی با همان امضا، بدون پاک‌کردن داده ارتقا دهید و سکه، تجربه، ستاره‌ها، ارتقاها و پهپاد انتخاب‌شده را قبل و بعد مقایسه کنید. APK دیباگ با امضای متفاوت جای نصب release را نمی‌گیرد.
- [ ] دود در نمای سوم‌شخص نازک و محو باشد؛ با بوست کمی تقویت و با تغییر به FPV کاملاً حذف شود، اما دود انفجار باقی بماند.
- [ ] موتور حداقل دو دقیقه در گاز ثابت بدون کلیک دوره‌ای شنیده شود. شروع، توقف، گاز کم و زیاد و بوست را با بلندگو و هدفون بررسی کنید.
- [ ] صدا را خاموش کنید و بین صفحه‌ها و مأموریت‌ها جابه‌جا شوید؛ صدای دیررس یا موتور قدیمی نباید شروع شود.
- [ ] هنگام انفجار، فلش کوتاه، حلقهٔ زمینی، ذرات و لرزش متناسب دیده شوند؛ HUD ثابت و خوانا بماند.
- [ ] در پیچ و تغییر دوربین، نشانگر به هدف بچسبد. نزدیک ساختمان و زمین، مرز نزدیک دوربین و ترتیب هم‌پوشانی را بررسی کنید.
- [ ] چندلمسی: جوی‌استیک و گاز و بوست را همزمان بگیرید و انگشت‌ها را با ترتیب‌های مختلف بردارید؛ مالکیت کنترل عوض نشود.
- [ ] هنگام نگه‌داشتن بوست و هدایت، Pause، Home، قفل گوشی و بازگشت را آزمایش کنید؛ پس از ادامه بوست یا فرمان گیر نکرده باشد.
- [ ] با تیلت و جوی‌استیک هر دو حالت بازی کنید. در دستگاه فاقد شتاب‌سنج، جوی‌استیک موجود باید کار کند.
- [ ] بین چند هدف یک مرحله، هنگام آماده‌شدن پهپاد بعدی موتور متوقف و با پرتاب دوباره روشن شود.
- [ ] روی چهار محیط و چهار مدل، رنگ آسمان، سایه، نور بدنه و ملخ را بررسی کنید. برای مدل‌های بدون ملخِ نمایشی، ملخ تازه اضافه نشده است.
- [ ] انتهای لیدربورد قابل اسکرول باشد و چرخاندن گوشی به جهت افقی دیگر، محدودهٔ اسکرول را خراب نکند.
- [ ] گوشی را با زبان فارسی، انگلیسی و عربی آزمایش کنید؛ ارقام HUD و زمان از مسیر تبدیل فارسی نمایش داده شوند.
- [ ] حداقل بیست بار بین منو، آشیانه و بازی رفت‌وبرگشت کنید و چند مأموریت را دوباره شروع کنید. با profiler دستگاه، heap و تعداد تردهای صوتی را مشاهده کنید؛ انتظار جهش دائمی و نامحدود نیست، اما اینجا اندازه‌گیری نشده است.
- [ ] روی دستگاه ضعیف و نرخ‌های متفاوت صفحه، پرواز و چند انفجار را آزمایش کنید. اگر عملکرد افت کرد، ابتدا لایهٔ جزئیات زمین و محوشدگی ملخ را با نسخهٔ پایه مقایسه کنید.

## ۱۰. فهرست فایل‌های تغییرکرده

| فایل | تغییر اصلی |
|---|---|
| `MainActivity.kt` | اتصال توقف و ادامهٔ صدا و پاک‌سازی چرخهٔ عمر |
| `core/BaseScreen.kt` | مالکیت انگشت، لغو لمس و توقف واقعی update |
| `core/EngineWave.kt` | فایل جدید؛ تولید موج دوره‌ای موتور |
| `core/GameAudio.kt` | سنتز پس‌زمینه، صف محدود، حلقه و آزادسازی ترک |
| `core/Fa.kt` | Locale ثابت پیش از تبدیل رقم |
| `core/Fonts.kt` | پشتیبانی از نام استاندارد فایل وزیرمتن با fallback |
| `core/Ui.kt` | cache مشترک تصویر، گرادیان cacheشده و آلفای متن |
| `game/Fx.kt` | دود مستقل موتور، آلفا، flash، حلقه و pool محدود |
| `game/Scene3D.kt` | نور مشترک، آلفا، کاهش تخصیص و پاک‌سازی |
| `game/SceneryArt.kt` | آسمان، خورشید، جزئیات زمین و سایه |
| `game/DroneArt.kt` | درز پنل، نور جهت‌دار و ملخ محوشده |
| `game/WorldRenderer.kt` | زمان‌بندی افکت‌ها و اتصال نمایش |
| `screens/GameScreen.kt` | کنترل ایمن‌تر، HUD و هماهنگی افکت‌ها |
| `screens/LeaderboardScreen.kt` | اسکرول بر اساس اندازهٔ محتوا |
| `.github/workflows/build.yml` | گام کنترل اندازهٔ APK دیباگ |

فایل‌های گزارش همراه: همین `CHANGES-0.0.4.md` و `REVIEW-VALIDATION.json`.
