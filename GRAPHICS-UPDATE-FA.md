# نسخهٔ گرافیکی ۰.۰.۳

این بسته بر پایهٔ فایل سورس ۰.۰.۲ ارسالی شما ساخته شده است. مخزن آنلاین شما تغییر نکرده است.

## تغییرات داخل بازی

- چهار تصویر جدید پهپاد با پس‌زمینهٔ شفاف و اندازهٔ ۵۱۲×۵۱۲، همراه تصاویر آشیانه.
- بال تصویردار در فضای سه‌بعدی، بدنهٔ چندوجهی سایه‌پردازی‌شده، سکان‌های برجسته و ملخ متحرک. مدل‌ها هنری و تقریبی هستند، نه مدل‌های مهندسی واقعی.
- حذف شبکهٔ تخت زمین و جایگزینی آن با بافت‌های تکرارشوندهٔ صحرا، شهر، دریا و ارتفاعات. پرسپکتیو بافت به مختصات جهان متصل است و هنگام حرکت دوربین می‌لغزد یا ثابت روی صفحه نمی‌ماند.
- آسمان روشن، نوار کوه‌های دور با تصویر محوشونده و مه فاصله.
- نما و سقف بافت‌دار ساختمان‌ها، جزئیات بام، خیابان و خط‌کشی شهری.
- تصاویر بزرگ‌تر درخت و سنگ؛ اندازهٔ روی صفحه تابع فاصلهٔ واقعی از دوربین است.
- تصاویر محیط و پهپاد در یک صف عمق با ساختمان‌ها رسم می‌شوند، نه همگی روی ساختمان‌ها. این روش همچنان ترتیب میانگین عمق دارد و جایگزین z-buffer نیست.
- برش سطوح جامد در صفحهٔ نزدیک دوربین، به‌جای حذف کل ساختمان با عبور یک رأس از دوربین.
- چهار تصویر جدید محیط در انتخاب فصل و بریفینگ، تصویر جدید منوی اصلی.
- بارگذاری یک‌بارهٔ تصاویر، حفظ شفافیت، ثبت خطای تصویر و حذف recycle ناامن.

## چیزهایی که دست نخورده‌اند

منطق پرواز، برخورد، دشمن‌ها، اهداف مراحل، امتیاز، ارتقا، صدا، کنترل‌ها و فرمت ذخیرهٔ پیشرفت تغییر نکرده‌اند. هیچ کتابخانهٔ اجرایی یا مجوز اینترنتی اضافه نشده است.

## محدودیت صادقانه

موتور همچنان Kotlin + Android Canvas است: رندر سبک ۲٫۵/سه‌بعدی با تصاویر طبیعی‌تر، نه موتور فوتورئال PBR. تصاویر مناظر تولیدشده با هوش مصنوعی هستند و تصویر واقعی مکانی مشخص نیستند. زمین برخورد همچنان مسطح است؛ کوه دور فقط نمای بصری دارد. پس‌پردازش PBR، نورپردازی حجمی و سایهٔ واقعی لحظه‌ای وجود ندارد. دود و ذرات همچنان در پاس نهایی اصلی بازی رسم می‌شوند و پشت ساختمان‌ها دقیق پنهان نمی‌شوند. تصاویر شفافِ عبوری از صفحهٔ نزدیک هنوز حذف می‌شوند؛ رفع برش فعلی برای سطوح جامد است.

## ساخت و نصب

محیط تولید این بسته Android SDK، Gradle و کامپایلر Kotlin نداشت و دسترسی اینترنت هم ندارد؛ APK ساخته نشده و تست روی دستگاه انجام نشده است. بررسی‌های همراه صرفاً سلامت منابع، ساختار کد و محاسبات مستقل پرسپکتیو هستند، نه تضمین کامپایل یا نرخ فریم.

روش راحت: فایل‌ها را در مخزن خود قرار دهید و در GitHub قسمت Actions گردش‌کار «Build APK» را اجرا کنید. این گردش‌کار debug APK و گزارش lint را به‌صورت artifact تحویل می‌دهد، به کلید انتشار نیاز ندارد و هیچ release قبلی را بازنویسی نمی‌کند. **رفتار گردش‌کار نسبت به نسخهٔ قبلی عمداً تغییر کرده است: دیگر انتشار خودکار signed release انجام نمی‌دهد.**

روش محلی: پوشهٔ پروژه را با Android Studio باز کنید. JDK 17، Android SDK 34 و Gradle 8.2 لازم است. باندل قدیمی فایل اجرایی gradlew و jar مربوط به wrapper را نداشت؛ این نسخه هم آن‌ها را جعل نکرده است. از Gradle نصب‌شده استفاده کنید:

```sh
gradle wrapper --gradle-version 8.2
./gradlew assembleDebug lintDebug
```

خروجی: `app/build/outputs/apk/debug/app-debug.apk`.
برای انتشار از keystore اصلی خودتان و تنظیمات `app/keystore.properties` استفاده کنید. نصب debug روی نسخهٔ release با کلید متفاوت ممکن نیست؛ برای حفظ داده‌ها نسخهٔ جدید را با همان کلید اصلی امضا کنید. پیش از هر حذف برنامه از داده‌ها پشتیبان بگیرید.

## چک‌لیست تست روی دستگاه

۱. هر چهار محیط را در نمای سوم‌شخص و FPV بررسی کنید.
۲. به چپ و راست بچرخید، اوج بگیرید و شیرجه بروید؛ جهت پهپاد و بافت زمین باید پایدار باشد.
۳. از نزدیک سنگ، درخت و ساختمان رد شوید و ترتیب پوشانندگی و برش نزدیک دوربین را ببینید.
۴. هر چهار پهپاد را در آشیانه و پرواز بررسی کنید.
۵. توقف/ادامه، تغییر اندازه، برگشت به منو، ادامهٔ ذخیره و حالت بی‌نهایت را بررسی کنید.
۶. نرخ فریم و مصرف حافظه را روی یک دستگاه ضعیف اندازه بگیرید. هدف عملکردی تعیین نشده و ۶۰ فریم تضمین نشده است.

## بازتولید تصاویر

فایل‌های اصلی تصاویر در `art-source/` هستند. این پوشه و `tools/` در APK بسته‌بندی نمی‌شوند. با Python 3، Pillow و numpy:

```sh
python tools/build_visual_assets.py art-source/drone-sheet.png art-source/environment-sheet.png
python tools/verify_visuals.py
```

تمام ۲۶ فایل PNG در assets/gfx با این بسته همراه‌اند. حافظهٔ خام RGBA تصاویر حدود ۱۷٫۴ MiB است؛ این عدد کل حافظهٔ برنامه یا تضمین مصرف نیست. فهرست SHA-256 و گزارش بررسی در `checks/` قرار دارد.
