# تغییرات نسخه 0.0.8

بیلد بعد از ممیزی رندر ساختمان‌ها و دریا. همهٔ اصلاحات زیر ریشهٔ قابل‌بازتولید
داشتند و با تست آفلاین قفل شده‌اند. هیچ وابستگی و مجوز جدیدی اضافه نشده و
`Kotlin + Canvas` مثل قبل است.

## ساختمان‌ها

- **شبکهٔ پنجرهٔ دوگانه حذف شد (علت اصلی به‌هم‌ریختگی نما)**: خودِ بافت دیوار یک
  شبکهٔ پنجرهٔ پخته‌شده داشت. روی ساختمان ۳۴ متری، شبکهٔ `۶٫۸m` بافت دقیقاً زیر
  شبکهٔ `۲٫۹m` رندر می‌افتاد و دو الگوی ناهم‌مقیاس روی هم می‌ساختند. حالا بافت
  دیوار بتنِ ساده با درزِ پنل است و فقط decalهای زمان اجرا پنجره می‌سازند.
- **قاب پنجرهٔ رخ `-Z` دیده نمی‌شد**: آفست قاب یک مقدار ثابت `+0.004f` بود و
  قاب رخ پشتی را داخل دیوار می‌برد، جایی که مرتب‌سازی رنگ آن را پنهان می‌کرد.
  آفست حالا با `side` هم‌جهت است و همیشه به بیرون همان رخ اشاره می‌کند.
- **سیلوئت ساختمان از «جعبهٔ صاف» جدا شد**: جان‌پناه چهارطرفه، یک واحد فنی
  و لولهٔ آب روی پشت‌بام (فقط در نزدیک‌ترین LOD، تا ساختمان از بالا هم متفاوت باشد).

## دریا و محیط دریایی

- **درز تکرار دریا**: دوره‌های موج قدیمی (`۱۷۰/۹۶/۳۴/۲۵` پیکسل) هیچ‌کدام tile
  ۵۱۲px را تقسیم نمی‌کردند. wave vectorهای صحیح جایگزین شد و باقی‌ماندهٔ پیوستگی
  به `۱e-14` رسید.
- **کف سفید (foam) از صفر به واقعی**: آستانهٔ دستی کف‌ها هیچ‌وقت فعال نمی‌شد؛ با
  percentile واقعی شیب/ارتفاع جایگزین شد.
- **پالت دریا روشن‌تر و پهن‌تر شد** تا کف‌های سفید واقعاً به سفید برسند؛ بازهٔ
  روشنایی از ۱۱۲ به ۱۶۵ رفت.
- **برش افق اشتباه (بدترین باگ)**: یک برش ثابت ۴۰٪ برای هر چهار محیط استفاده
  می‌شد. اندازه‌گیری روی خودِ PNGها نشان داد افق دریا روی ردیف ۲۴٪ و افق شهر
  روی ۲۶٪ است؛ یعنی بازی **آسمان خالی** رای دریا نشان می‌داد و **شهر و کوه** را
  روی زمینِ خودش می‌کشید. `skyFraction()` حالا برای هر محیط مقدار اندازه‌گیری‌شده
  دارد: دریا ۰٫۲۴، شهر ۰٫۲۵، ویژه ۰٫۶۷، بیابان ۰٫۵۵. (اول یک آشکارساز لبهٔ خودکار
  نوشته شد ولی روی ۳ از ۴ تصویر اشتباه گرفت — روی لبهٔ ابر قفل می‌شد — پس به
  ثابت‌های اندازه‌گیری‌شده و قفل‌شده با تست رفتیم.)
- **دریا بی‌حرکت بود**: حالا با `seaPhase*3.4` در جهت باد می‌لغزد و لایهٔ
  جزئیات هم همان drift را می‌گیرد تا دو میدان موج روی هم نیفتند.

## تست

- `checks/render/run.py`: هارنس رندر واقعی (Kotlin ۱٫۹٫۲۲ + JDK ۱۷) — ۶ تست رندر
  + ۸ تست هندسهٔ نما، همه سبز.
- `checks/sea_texture.py`: ۶ تست بافت دریا (پیوستگی، foam، بازهٔ روشنایی، گلینت).
- `checks/horizon_sheet.py`: ۴ تست برش افق، مقادیر `skyFraction` را در برابر خودِ
  PNGها قفل می‌کند.
- کامپایل کامل همهٔ سورس‌های تولیدی Kotlin در برابر `android.jar` واقعی: بدون خطا.

مجموعاً ۲۴ تست سبز. تست روی دستگاه واقعی انجام نشده (FPS/لمس/ظاهر نهایی باید روی
گوشی دیده شود).
