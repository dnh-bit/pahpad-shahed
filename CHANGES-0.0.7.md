# تغییرات نسخه 0.0.7

## فیکس گیم‌پلی

- **شرط ستارهٔ دوم عوض شد**: «میانگین دقت برخورد ۵۰٪ یا بیشتر» که عملاً قابل گرفتن نبود
  حذف شد و به «جمعِ همهٔ بونوس‌های مخفی مسیر» تبدیل شد. بریفینگ، صفحهٔ نتیجه و
  متن‌های راهنما به‌روز شدند. ماموریت بدون بونوس، این شرط را خودکار می‌گیرد.
- **نوار سوخت اول هر مرحله کم نمی‌شد (پین روی ۱۰۰٪)**: سوخت اولیه عمداً بزرگ‌تر از
  حداکثر برد است (حداقل ۱٫۴۵× فاصله ماموریت + ۱۵٪ ذخیره) ولی نوار نسبت به
  `maxRange` سنجیده می‌شد؛ حالا نسبت به سوخت آغاز همان پهباد سنجیده می‌شود و بلافاصله
  بعد از پرتاب حرکت می‌کند. امتیاز «مدیریت سوخت» هم به همان مبنا اصلاح شد.
- **حالت بی‌نهایت**: با هر موج، بونوس‌های جا‌ماندهٔ موج قبلی (غیرقابل‌دسترس پس از
  تعویض دنیا) از مخرج امتیاز/ستارهٔ بونوس حذف می‌شوند تا شرط «جمع همهٔ بونوس‌ها»
  در بی‌نهایت هم قابل گرفتن باشد.

## فیکس گرافیک (ادامهٔ 0.0.6)

- **پنجرهٔ ساختمان‌ها بازطراحی شد**: به‌جای یک نوار کشیده روی دو رخ، حالا روی هر چهار
  رخ، شبکهٔ پنجره با مقیاس واقعی (۲٫۱۵×۲٫۴۵ متر، طبقات ۳٫۵۵ متری) + قاب نازک تیره +
  تنوع روشن/تاریک (در عملیات شبانه برخی پنجره‌ها روشن) چیده می‌شود؛ با LOD و حالت
  ویرانی سازگار است و سیلوئت ساختمان را تغییر نمی‌دهد.

## تست

- `StarRulesTest`: شرط جدید ستارهٔ دوم، نبودِ شرط قدیمی، نمایش شمارش واقعی بونوس.
- `MissionFuelTest`: نوار سوخت باید بلافاصله بعد از پرواز از ۱۰۰٪ پایین بیاید (رجرسیون باگ پین).
- همهٔ تست‌های قبلی (رندر، ساعت فریم، چیدمان شهر، برخورد پرتابه) سر جایشان.

## APK مرحلهٔ Godot

- `spikes/godot-flight` حالا export اندروید دارد (`export_presets.cfg`) و CI یک ریلیز
  جدا می‌سازد: `saffron-sky-v0.0.7-godot-flight.apk` با شناسهٔ بستهٔ مجزا
  `ir.shahed.pahpad.godotflight` تا کنار بازی اصلی نصب شود. امضا debug است و هدفش
  تست روی گوشی است، نه انتشار فروشگاهی.
